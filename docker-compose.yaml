services:
  tg_bot:
    container_name: tg_bot_container
    build: ./src
    restart: unless-stopped

    command: python /src/run.py
    volumes:
      - ./src/:/src/
      - ./ssl/:/ssl/
    env_file:
      - .env
