services:
  gss_bot:
    build: ./src
    restart: always
    network_mode: host

    command: /entrypoint.sh
    mem_limit: 500m
    memswap_limit: 800m
    volumes:
      - ./src/:/src/
      - ./ssl/:/ssl/
      - ./imgs/:/imgs/
      - ./logs/:/gss_logs/
    env_file:
      - .env