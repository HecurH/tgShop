services:
  gss_bot:
    build: ./src
    restart: always
    network_mode: host

    command: python /src/run.py
    mem_limit: 300m
    memswap_limit: 600m
    volumes:
      - ./src/:/src/
      - ./ssl/:/ssl/
      - ./imgs/:/imgs/
      - ./logs/:/gss_logs/
    env_file:
      - .env
