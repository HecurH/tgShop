services:
  gss_bot:
    container_name: gss_container
    build: ./src
    restart: unless-stopped
    network_mode: host

    command: python /src/run.py
    volumes:
      - ./src/:/src/
      - ./ssl/:/ssl/
      - ./logs/:/gss_logs/
    env_file:
      - .env
